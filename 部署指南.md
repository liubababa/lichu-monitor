# 力储未来 · MQTT broker 部署与联调指南

> 适用协议：中和汇能《晶农EMS的MQTT通讯协议》2026-03-04（Topic 前缀 `zhhn/`）；
> 高特 CCU 走 `/{ProductSN}/{DeviceSN}/rtg/…`，依据《高特MQTT使用手册V1.2》+ 项目文档 SJ2025B3781ESCCU-MQTT。
> 目标：让厂家设备与「力储未来」网页平台都连上同一个 broker，设备上报数据 → 页面实时显示。

## 线上现状（2026-09-18 起）

| 项 | 值 |
|---|---|
| 服务器 | 腾讯云轻量 `43.129.27.206`（实例名 lichu-mqtt，Ubuntu + mosquitto + nginx） |
| 域名 | `mqtt.ykdesign.top`；解析在**阿里云**云解析（A 记录 → 该 IP）。腾讯轻量的「域名解析」面板对该域名无效，别在那儿改 |
| 设备接入 | `1883/tcp` 明文（需账号密码）：`zhhn_ems`（晶农 EMS）、`gaote_ccu`（高特 CCU）、`lichu_web`（网页）、`lichu_log`（留痕服务） |
| 网页接入 | `wss://mqtt.ykdesign.top/mqtt`（nginx 反代 → **mosquitto 127.0.0.1:8083**）。账号 `lichu_web`（**必须填**，否则连不上；填了才能下发） |
| 报文留痕 | `mqtt-watch.service` 常驻订阅 `#` → `/var/log/mqtt-watch/messages.log`（按天轮转留 3 天）。查厂家有没有上来：`sudo tail -50 /var/log/mqtt-watch/messages.log`；连接/认证记录在 `/var/log/mosquitto/mosquitto.log` |
| 证书 | Let's Encrypt，certbot 自动续期；站点配置 `/etc/nginx/sites-available/mqtt.ykdesign.top` |
| 账号密码 | 不写进仓库：服务器 `/etc/mosquitto/passwd`；本地见《给厂家的接入参数》《给高特的接入参数》 |

> ⚠️ `ykdesign.top` 根域与 `www` 指向 GitHub Pages（网页就挂在那），**不要改动**这两条记录；只维护 `mqtt` 这一条子域。

## 零、方案记录：EMQX Cloud Serverless（已撤回，保留备查）

> **状态：2026-09-17 已按需求撤回部署。** 以下记录当时的做法与实测到的限制，重新部署时可直接复用。

**方案：EMQX Cloud Serverless（杭州节点，免费额度）** —— 不自建服务器，由 EMQX 官方托管。

```
晶农 EMS(现场)                EMQX Cloud Serverless               网页平台
 mqtts://<broker>:8883  ───►  <broker地址>                  ◄───  wss://<broker>:8084/mqtt
 （账号 zhhn_ems）                    ↑                            （账号 lichu_web）
                                     └─ EMQX 负责路由，双方互不直连
```

| 项目 | 说明 |
|---|---|
| broker 地址 | 部署后由 EMQX 控制台给出（形如 `xxxx.ala.cn-hangzhou.emqxsl.cn`），接入参数请勿写入代码或仓库 |
| 端口 | `8883` MQTT/TLS（给 EMS）、`8084` WebSocket/TLS（给网页） |
| 账号 | 建议建两个：给厂家 EMS 一个、给网页一个；密码用强密码，不要用弱口令 |
| 控制台 | https://cloud.emqx.com/console → 项目 → 部署 |
| 免费额度 | 每月 100 万连接分钟 + 1 GB 流量（1 台 EMS 全天在线约用 4.3 万分钟 / 170 MB，富余很大） |

**⚠️ 三个必须知道的限制（已实测）**

1. **单客户端最多 10 个订阅** —— 第 11 个起 broker 仍回 SUBACK 成功，但**静默不投递**。
   平台已把订阅收敛为 4 个（`zhhn/Post/Login/+`、`zhhn/Post/PeriodReport/+`、`zhhn/GetRsp/#`、`zhhn/SetRsp/#`），
   EMS 侧订阅请控制在 10 个以内。
2. **只有加密端口（8883/8084），没有明文 1883** —— EMS 必须支持 MQTT over TLS，否则需改用自建 broker（见第三节）或弹性专有版。
3. **免费额度用尽后部署会被停止** —— 建议在控制台「部署设置」把消费限额设成 ¥10，超额自动小额扣费，避免断服（实际用量约每月几毛钱）。

**日常运维**

- 看设备是否在线、消息量：控制台 → 部署 `lichu-monitor` → 监控
- 改账号密码：控制台 → 访问控制 → 客户端认证 → 编辑用户（改完记得同步更新网页配置面板）
- 收紧权限（建议联调稳定后做）：访问控制 → 客户端授权，从"黑名单模式"切到"白名单模式"，只放行 `zhhn/#`

**平台网页默认配置**（`js/data.js` → `APP_CONFIG.mqtt`）已指向上述 broker；
用户也可在页面右上角「数据源」面板里临时改地址/账号（存在浏览器 localStorage，仅本机生效）。

---

## 一、总体架构（以下为自建 broker 方案，EMQX Cloud 之外的可选方案）

```
                    ┌─────────────────── ykdesign.top 服务器 ───────────────────┐
晶农 EMS(工控机/4G) │  EMQX broker                                             │
   │  MQTT(TCP)     │   ├─ :1883  MQTT/TCP  ← EMS 连这里（配置第三方broker）    │
   └──────────────► │   ├─ :8083  MQTT/WS                                      │
                    │   └─ :18083 管理台(仅本机/内网访问)                        │
                    │  nginx                                                    │
                    │   └─ wss://ykdesign.top/mqtt ──反代──► 127.0.0.1:8083     │
                    └──────────────────────────────────────────────────────────┘
                                     ▲
                                     │ 浏览器 wss（mqtt.min.js）
                        「力储未来」网页（订阅 zhhn/#，收报文、回应答、下发）
```

要点：
- EMS 与网页**互不直连**，都以客户端身份连 broker。
- 网页在 HTTPS 站点下只能用 `wss://`，所以必须由 nginx 把 `/mqtt` 路径反代到 EMQX 的 WebSocket 端口（8083）。
- 平台网页收到 `zhhn/Post/Login/{SN}` 后会自动回 `zhhn/PostRsp/Login/{SN}`（result=1）——**EMS 登录成功后才开始周期上报**，所以网页端必须保持连接。

## 二、部署 EMQX（服务器上二选一）

### 方式 A：Docker（推荐）

```bash
docker run -d --name emqx \
  -p 1883:1883 -p 8083:8083 -p 18083:18083 \
  --restart always \
  emqx/emqx:5.8
```

管理台：`http://服务器IP:18083`，默认 `admin/public`（**首次登录立即改密码**）。

创建两个认证用户（管理台 → 访问控制 → 认证 → 内置数据库 → 添加）：

| 用户名 | 用途 | 建议密码 |
|---|---|---|
| `zhhn_ems` | 给 EMS 用 | 强密码，告知中和汇能现场配置 |
| `lichu_web` | 给网页用 | 强密码 |

### 方式 B：mosquitto（apt 直装，轻量）

```bash
apt install -y mosquitto
# /etc/mosquitto/conf.d/zhhn.conf
listener 1883
allow_anonymous false
password_file /etc/mosquitto/passwd
listener 8083
protocol websockets

# 生成密码文件
mosquitto_passwd -c /etc/mosquitto/passwd zhhn_ems
mosquitto_passwd -b /etc/mosquitto/passwd lichu_web <密码>
systemctl restart mosquitto
```

> ⚠️ **密码文件属主不能改**：`/etc/mosquitto/passwd` 保持 `mosquitto:mosquitto` + `0600`。
> 实测改成 `root:root 0600` 后服务直接起不来（`exit status=13`，mosquitto 降权到 mosquitto 用户后读不到文件）。
> mosquitto 启动时那句 `owner is not root ... future versions will refuse to load this file` 在本机环境下**不要照做**。
> 新增账号：`sudo mosquitto_passwd -b /etc/mosquitto/passwd <用户> <密码>` 后 `sudo systemctl reload mosquitto`（HUP 会重读密码文件，不用重启）。

### 防火墙 / 安全组

放行入站：`1883/tcp`（EMS 用）、`443/tcp`（网页 wss 走 nginx）。
**不要**对公网放行 `8083` 和 `18083`（18083 建议仅本机或加 IP 白名单）。

## 三、nginx 反代 WebSocket（wss）

在 `ykdesign.top` 的 HTTPS server 块里加：

```nginx
location /mqtt {
    proxy_pass http://127.0.0.1:8083;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_read_timeout 300s;
    proxy_send_timeout 300s;
}
```

重载：`nginx -t && nginx -s reload`。
验证：浏览器打开 `https://ykdesign.top/dianzhan/`，点顶栏「演示数据」→ Broker 地址已默认填 `wss://ykdesign.top/mqtt`，填上 `lichu_web` 的账号密码 → 连接 → 提示「MQTT 已连接」。

## 四、EMS 侧配置（告知现场）

在 EMS 本地配置「第三方 broker」：

| 项 | 值 |
|---|---|
| 服务器地址 | `ykdesign.top`（或服务器公网 IP） |
| 端口 | `1883`（TCP，明文） |
| 用户名/密码 | `zhhn_ems` / 对应密码 |
| 上行 Topic | 按协议：`zhhn/Post/Login/{SN}`、`zhhn/Post/PeriodReport/{SN}` |

EMS 上电后会主动发 Login；平台网页回 PostRsp 后进入周期上报（各设备独立周期，约 10 分钟汇聚一次，见协议第 2 节）。

### 厂家仅支持 1883 明文时的说明

厂家明确 EMS 只能发 1883 明文时，**方案照旧可用**：EMS 走 1883 明文、网页走 wss 加密，两侧互不影响。

安全要求（明文经公网传输，必须做到）：
1. **禁用匿名 + 开启账号密码认证**（建 `zhhn_ems`、`lichu_web` 两个账号，见第二节）
2. 建议在服务器防火墙对 1883 端口做**来源 IP 白名单**（若储能站有固定出口 IP）
3. 可选加固：后续厂家固件若支持 TLS(8883)，再补开加密端口即可，无需换方案


## 五、联调验证步骤

1. **页面连 broker**：顶栏数据源 → 填 `wss://mqtt.ykdesign.top/mqtt` → 账号密码填 `lichu_web` / 对应密码 → 连接 → 状态变「MQTT 已连接 · 等待上报」。
2. **等 EMS 上线**：报文日志出现 `↓ zhhn/Post/Login/{SN}`，**页面在线会自动回** `↑ zhhn/PostRsp/Login/{SN}`（result=1）。⚠️ 页面关着就没人回，EMS 会停止上报（高特 CCU 不需要登录应答）。
3. **数据到达**：随后出现 `↓ zhhn/Post/PeriodReport/{SN}`，系统概况/SOC/曲线/3D 标签开始显示真实值；KPI 下的小字会标注电量口径（厂家日点位 or 会话积分）。
4. **主动召测**（可选）：配置面板填设备 SN → 点「实时召测（60 帧）」，EMS 每 5 秒回 1 帧共 60 帧。
5. **下发验证**（可选）：「充放电策略」页签填参数 → 下发 EmsSet → 看 SetRsp 的 result/errormsg。⚠️ 先在空载/测试站验证。
6. **设备清单**：配置面板点「设备清单」，弹出 Get/DeviceInfor 应答表格。

## 六、没有 EMS 时的本地模拟（推荐：内置工具）

`tools/` 目录自带两个联调工具（依赖见 `tools/package.json`，首次使用先 `cd tools && npm i`）：

```bash
# 1. 本地 broker：TCP :1884 给模拟器，WS :9001 给网页
node tools/local-broker.js

# 2. 高特 CCU 模拟器（周期 5 秒便于观察；协议正常是 30 秒）
node tools/ccu-simulator.js --url mqtt://127.0.0.1:1884 --dsn SN2025B3781TEST --interval 5

# 3. 静态站点（别用 file:// 双击打开）
cd H:\dianzhan && python -m http.server 8080 --bind 127.0.0.1
```

网页 Broker 地址填 `ws://127.0.0.1:9001/mqtt`（本地 http 页面才能填 `ws://`），协议选「高特 CCU」，
ProductSN 留空（默认 `kp23bhcpmt91n2v8`），设备 SN 填 `SN2025B3781TEST` → 连接。
模拟器含 EMU/PCS/堆/簇/单体/电表/液冷/除湿/消防 全部点位。

也可以用 mosquitto_pub 手动发单条报文（把 SN 换成实际值）：

```bash
B=tcp://ykdesign.top:1883; U=zhhn_ems; P=<密码>; SN=SN21881FFF0001

# 1. 登录
mosquitto_pub -h ykdesign.top -p 1883 -u "$U" -P "$P" -t "zhhn/Post/Login/$SN" \
  -m '{"identifier":"Login","vendor":"zhhn","time":'$(date +%s)'}'

# 2. 周期上报（EMS 汇聚多设备：系统参数 / PCS / BMS / EMS 参数）
mosquitto_pub -h ykdesign.top -p 1883 -u "$U" -P "$P" -t "zhhn/Post/PeriodReport/$SN" \
  -m '{"identifier":"PeriodReport","SN":"'$SN'","data":[
        {"deviceType":"系统参数","tags":{"SN":"'$SN'","CpuUsage":"12","netIp":"10.0.0.5","version":"工控机版V1.6"}},
        {"deviceType":"PCS","tags":{"AphaseVoltage":"233.8","ActivePower":"-150.5","RunStatu":"257","Online":"1"}},
        {"deviceType":"BMS","tags":{"RackVoltage":"707.1","SOC":"86.5","RackMaxTemp":"31","Online":"1"}},
        {"deviceType":"EMS","tags":{"PCS_P":"-150.5","LoadPower":"420.0","GRID_P":"270.0","BMS_SOC":"86.5","CabPower":"500","CabCapacity":"1000","SysStatus":"0","Online":"1"}}
      ],"time":'$(date +%s)'}'
```

页面应在数秒内显示 SOC 86.5%、PCS 曲线出现 -150.5kW（负值=充电中）、设备状态「充电中」。

## 七、常见问题排查

| 现象 | 排查 |
|---|---|
| 网页连不上 wss | ① 网页里填的是 `wss://` 而不是 `ws://`（https 页面只能连 wss）；② nginx 反代是否生效：`grep -A1 'location /mqtt' /etc/nginx/sites-available/mqtt.ykdesign.top` 应指向 `127.0.0.1:8083`；③ `systemctl status nginx mosquitto` |
| **设备一直不上线**（最常遇到的） | 见下方「设备没连上来怎么查」：先看 broker 日志有没有连接记录，再看抓包有没有包打进来。**两者都空 = 设备的包根本没出它的网络**，问题在设备侧（域名解析/端口/物联网卡） |
| Login 后无数据 | EMS 只在收到 result=1 的 PostRsp 后才上报。晶农协议需要有人应答：页面开着时会自动回（网关已停用，不再代为应答）；高特 CCU 不需要登录应答 |
| 网页下发没反应 | 确认已填 `lichu_web` 账号密码；看报文日志里有没有 `↑ .../cmd/set/...` |
| 报文显示但页面无数值 | 看「报文日志」里 payload 的 `deviceType` 是否为点表设备（系统参数/PCS/BMS/EMS…）；无效点会显示 `--` 或 `Bad` |
| 数据 10 分钟才动一次 | 正常——晶农周期上报为汇聚上报；高特是按主题 30 秒上报 |

### 设备没连上来怎么查（三段定位）

```bash
node tools/remote-run.js --file tools/diagnose.sh    # 一键体检：三段全查一遍
```

| 查什么 | 怎么看 | 结论 |
|---|---|---|
| ① 设备连过 broker 吗 | `sudo grep 'New client connected' /var/log/mosquitto/mosquitto.log \| grep -v 127.0.0.1` | 有 = 连过；空 = 从来没连过 |
| ② broker 收到报文了吗 | `sudo tail -5 /var/log/mqtt-watch/messages.log` | 有 = 数据到了 |
| ③ 是不是被防火墙拦了 | 从外网 `Test-NetConnection 43.129.27.206 -Port 1883`：秒拒=放行但没服务；**超时=被拦** | 现在 1883/8883 都是放行的 |
| ④ 任意端口有没有包进来 | 服务器上 `sudo tcpdump -i any -n 'tcp[tcpflags] & tcp-syn != 0 and not host 127.0.0.1'` | 能看到 SYN = 包到了服务器 |

> 关键判据：**端口填错 ≠ 没有痕迹**。填错端口包也会到服务器（能看到 SYN、只是被拒）；什么都没看到，说明设备的连接请求没有发出来 —— 查设备侧（域名解析、端口、物联网卡白名单）。

## 八、前后分离网关（**已停用**，保留备查）

> ⚠️ **2026-09-19 已停用**：为了减少排查层级（链路从 4 段变 3 段），nginx 的 `/mqtt` 已改回直连 `mosquitto:8083`，`mqtt-gateway.service` 已 `disable --now`（文件与配置保留）。
> 停用代价：① 网页里必须填 `lichu_web` 账号密码（原来网关可匿名只读）；② 晶农 EMS 的登录应答改回"页面在线才回"（高特 CCU 不受影响，它不需要登录应答）。
> 恢复方式：`sudo systemctl enable --now mqtt-gateway` + 把 nginx 的 `proxy_pass` 改回 `http://127.0.0.1:8090`，然后 `nginx -s reload`。
> 顺带说明：网关的 `retainLast` 会把每个主题的最后一条报文缓存并回放给新开的页面 —— 联调阶段容易看到"过期/测试数据"，这也是本次停用它的原因之一。

> 2026-09-19 新增（现已停用）。目的：broker 账号不进浏览器、页面关着也能应答设备登录、新开页面立即有数据、匿名访客只能看不能控。

```
厂家设备(EMS/CCU) ──1883/8883──► mosquitto ──► 网关 gateway.js ──wss /mqtt──► 网页
                                     ▲                │
                                     └── 平台下发 / 登录应答 ──┘
```

- 网页仍然用 MQTT-over-WebSocket（浏览器只能这么连），但连的是**网关**；网关再以普通 MQTT 客户端身份连 broker（TCP，不需要 WS 端口）。
- broker 账号只写在服务器的 `/opt/mqtt-gateway/config.json`（0600，不进仓库）。
- **登录应答由网关自动回复**：页面全关着，EMS 也不会因为没人回 PostRsp 而停止上报（原先必须有人开着页面）。
- 网关保留各主题最后一条报文，新打开的页面立即显示当前值，不用等下一个上报周期。
- 鉴权：匿名 = 只读（能订阅，下发被静默丢弃、不断开连接）；填了 `users` 里的账号才可下发。

### 相关文件

| 文件 | 作用 |
|---|---|
| `tools/gateway.js` | 网关本体（aedes 供网页侧 MQTT-over-WS + mqtt 客户端连上游） |
| `tools/gateway.config.example.json` | 配置模板；服务器配置放 `tools/gateway.config.server.json`（含密码，不进仓库） |
| `tools/deploy-gateway.js` | 一键部署：上传 → 装 Node/依赖 → 建 mosquitto 账号 → systemd → nginx 改指 → 自测 |
| `tools/test-gateway.js` | 本地端到端测试（13 项断言，不依赖外部服务） |

### 部署（在本地项目目录执行）

```bash
node tools/test-gateway.js            # 先本地跑通
node tools/deploy-gateway.js          # 读 tools/gateway.config.server.json，部署到服务器
```

部署脚本会：建 `/opt/mqtt-gateway`（gateway.js + config.json + package.json）、`npm i`、用配置里的 upstream 账号执行
`mosquitto_passwd -b` 建/更新账号、装 `mqtt-gateway.service`（开机自启、崩溃重拉）、把 nginx `mqtt.ykdesign.top` 的
`/mqtt` 由 `8083` 改指 `8090`（mosquitto 的 WS 端口保留，随时回滚）并新增 `/healthz`，改 nginx 前自动备份；
最后自测：本机 `/healthz`、网页侧 WS 登录+回环、`https://mqtt.ykdesign.top/healthz`。

### 日常查看

```bash
systemctl status mqtt-gateway               # 运行状态
journalctl -u mqtt-gateway -n 50            # 最近日志（登录应答、网页接入、只读拦截都记在这里）
curl -s https://mqtt.ykdesign.top/healthz   # 上游连接、消息数、在线网页数
```

### 回滚（回到网页直连 broker）

```bash
sudo cp /etc/nginx/sites-available/mqtt.ykdesign.top.bak.* /etc/nginx/sites-available/mqtt.ykdesign.top
sudo nginx -t && sudo systemctl reload nginx
sudo systemctl disable --now mqtt-gateway
```

回滚后网页需重新填 `lichu_web` 账号密码（旧直连方式要账号），且只有页面在线时才有人回登录应答。
