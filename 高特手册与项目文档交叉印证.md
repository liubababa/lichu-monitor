# 高特手册 ↔ 项目协议文档 交叉印证（含电表部分）

> 比对对象：③《高特MQTT使用手册》V1.2.pdf ↔ ④《SJ2025B3781ESCCU-MQTT.xlsx》
> 说明：本次只比对高特这一对（晶农那套两份文档暂不涉及）

## 一、两份文件的定位（先说清楚关系）

| 文件 | 定位 | 关键提示 |
|---|---|---|
| ③ 手册 | **通用说明 + 示例**，讲"怎么对、注意什么" | 第 1 节明确写：**"MQTT 具体的主题和点位因不同的项目而不同，每个项目有对应的 MQTT 协议文档。本文以『示例-MQTT.xlsx』为例"** |
| ④ 项目文档 | **本项目实际的点位表**（SJ2025B3781） | 首表标注：项目名"中和汇能-山东125kW261kWh工商业液冷储能项目"、格式版本 V2、模板 V1.7.5、生成于 2025-11-12 |

**推论**：手册里的主题/点位都是**示例项目**的，不能直接当作本项目的真值；本项目以 ④ 为准。这一点与我们实际对接方式一致（平台按 ④ 解析）。

## 二、✅ 逐项一致的部分（互相印证成立）

| 项 | ③ 手册 | ④ 项目文档 | 结论 |
|---|---|---|---|
| 主题宏体系 | `${ProductSN}` `${DeviceSN}` `${arrNum}` `${CluNum}` `${devIndex}` `${ManuModel}` `${CompAlgo}` | 主题定义表逐条使用同一套宏 | **一致** ✔ |
| 上报周期 | "传输模式…周期传输，传输周期为 30000ms" | 每条主题的传输参数列 = **30000** | **一致** ✔ |
| 数据格式 | "JSON/protobuf 字段列为 KEY，其它列说明名称/类型/单位/功能" | 遥测/遥信表列结构完全相同（JSON/protobuf字段、属性标识、名称、类型、单位…） | **一致** ✔ |
| 压缩算法 | comp-gzip / comp-lz4 | 压缩算法工作表：comp-gzip / comp-lz4 | **一致** ✔ |
| 无效值 | 6.2 节：**65535 表示无效**（设备与 BCM 失联等） | 各点位最大值/取值范围内未再声明（属通用约定） | **补集关系**，不冲突 ✔ |
| 正负约定 | 6.8 节：**正放负充**（CCU 默认） | 遥调表 dispatchPower 有功功率 kW（符号取此约定） | **一致** ✔ |
| 平台配置项 | 4.1.3 参数说明（账号/密码/用户ID/主机地址/密钥/平台ID） | 属平台配置，不在点位表内 | **互补** ✔ |
| 下发流程 | 6.1 节：控制源切远程 → `powerCmd` 设模式 → 具体指令 → 设备回读 `cmd/get` | 遥调表给出每个功能的 get/set/history 三条主题 | **一致** ✔（④ 提供完整主题清单） |
| 断网补传 | 4.4 节：本地最多存 7 天，恢复后补传到"上行历史主题" | 每条主题都有对应的 **EMS上行历史主题** 列 | **一致** ✔ |

## 三、⚠️ 不一致的部分（重点，直接影响能不能收到数据）

### 1. 主题形状不同（手册示例 vs 本项目）

| 数据 | ③ 手册示例（第 5 章） | ④ 项目文档（主题定义） |
|---|---|---|
| 堆遥测 | `/…/rtg/data/`**`arr`**`/0`（1 个参数） | `/…/rtg/data/`**`array`**`/${arrNum}/${CluNum}/${devIndex}`（3 个参数） |
| 簇遥测 | `/…/rtg/data/`**`clu`**`/0/0`（2 个参数） | `/…/rtg/data/`**`cluster`**`/${arrNum}/${CluNum}/${devIndex}`（3 个参数） |
| 电表遥测 | `/…/rtg/data/meter/0/-1/1`（**无型号段**） | `/…/rtg/data/meter/`**`{aems\|mems\|lems}`**`/`**`{storage\|antireflux\|demand}`**`/${arrNum}/${CluNum}/${devIndex}`**`/extend/${ManuModel}`** |
| 电表遥信 | `/…/rtg/status/meter/0/-1/3` | 同上，带 `extend/${ManuModel}` |

**判断**：手册第 1 节已说明"主题因项目而异"，所以这属于**示例与实际的差异**，不算文档错误。但对我们的影响是实的：
- 若**按 ④ 实现**（我们现在的做法）→ 设备若真发手册那种简写，我们会收不到
- 若**按 ③ 实现** → 本项目点位又对不上

**处理**：平台已做**双向兼容**（堆/簇的 `arr`/`clu` 简写与 `array`/`cluster` 全写都能识别；电表两种段式都会尝试匹配）。
**仍需厂家确认**：现场固件实际发的是哪种形状（建议让他们抓一条真实报文给我们）。

### 2. 示例值千万不能当真

| 位置 | 出现的值 | 说明 |
|---|---|---|
| ③ 手册 5.1.1–5.1.4 | `ProductSN = kp23bhcpmt91n2v8`、`DeviceSN = S230612B0125` | **示例项目** |
| ③ 手册 5.1.5–5.1.6 | `DeviceSN = R221010B2001` | 同一手册里换了另一台示例设备 → 证明都是示例 |
| ③ 手册 4.1.3 | 密钥默认 `RFyim2I3IcfK`、用户ID 默认 `{deviceId}`、平台ID 默认 0 | 出厂默认，本项目沿用与否需确认 |
| ⑤ 设备截图 | 密钥显示 `RfymI2I3IcIK` | 与手册字符不完全一致 → **以设备实际为准，不要手改** |

### 3. 大小写与拼写

- ③ 手册写 `t4GInfo`，④ 遥测表维度写 `t4ginfo` → 平台已按**大小写不敏感**处理

## 四、电表部分详解（④ 项目文档）

### 4.1 四类电表（各是独立的数据维度）

| 数据维度 | 中文名（设备拓扑） | 遥测点数 | 遥信点数 | 用途 |
|---|---|---|---|---|
| `meter-aems-storage` | AEMS储能计量电表 | 45 | 7 | 储能侧计量 |
| `meter-mems-storage` | MEMS计量电表 | 35 | 7 | 计量柜 |
| `meter-lems-antireflux` | LEMS逆流电表 | 39 | 7 | 并网点防逆流 |
| `meter-lems-demand` | LEMS需量电表 | 35 | 7 | 需量控制 |

### 4.2 电表点位（以 AEMS 计量电表为例，其它维度同族）

| 类别 | 点位 |
|---|---|
| 频率 | `meter_f`（频率 Hz） |
| 三相电压/电流 | `meter_a_vol` / `meter_b_vol` / `meter_c_vol`；`meter_a_cur` / `meter_b_cur` / `meter_c_cur` |
| 线电压 | `meter_ab_vol` / `meter_bc_vol` / `meter_ca_vol` |
| 分相功率 | `meter_a_p` / `meter_b_p` / `meter_c_p`；`meter_a_q` / `meter_b_q` / `meter_c_q`；`meter_a_s` / `meter_b_s` / `meter_c_s` |
| 总功率 | `meter_tot_p`（总有功 kW）/ `meter_tot_q`（总无功 kvar）/ `meter_tot_s`（总视在 VA） |
| 功率因数 | `meter_a_pf` / `meter_b_pf` / `meter_c_pf` / `meter_tot_pf` |
| 电能 | `meter_add_pos`（当前正向有功总电能）、`meter_add_neg`（反向）、`meter_add_p`（组合有功）、`meter_add_q`（组合无功） |
| 分时电量 | 尖/峰/平/谷：`meter_ver_pos` `meter_hig_pos` `meter_mid_pos` `meter_low_pos`（正向）与对应 `_neg`（反向）、`meter_dplow_*`（深谷） |
| 变比 | `meter_pt`（PT变比）、`meter_ct`（CT变比） |
| 结算日 | `meter_1day_pos_eng` / `meter_1day_neg_eng`（上一结算日正/反向有功总电能） |

### 4.3 电表型号与 `extend` 段（重要）

④ 主题定义里电表的**生效匹配**列列出同一维度下的多种型号，实际用哪个由主题末段的 `${ManuModel}` 决定：

| 维度 | 出现过的型号（设备拓扑 / 生效匹配） |
|---|---|
| AEMS储能计量 | acrel-dtsd1352、acrel-adl400、acrel-adw300、dlt645-2007、meter-null |
| LEMS逆流 | acrel-adl400-v13、acrel-amc1.4-v1、acrel-adw300-v6、acrel-dtsd1352-v19、sfere-pd194ze20x-v1、willfar-dtsd342-v2、meter-null |
| LEMS需量 | acrel-amc1.4-v1、meter-null |
| MEMS计量 | acrel-amc1.4-v1 |

→ 平台解析时**按 `extend` 后的型号做通配匹配**（同维度不同型号点位一致），无需为每个型号单独配置。

### 4.4 电表相关的待确认

1. 本项目**实际装了哪几块表、对应哪个型号**（决定 `extend` 段的值）
2. 电表的**堆/簇/设备号**取值（主题里的三个参数；设备拓扑显示 AEMS/LEMS 为 `-1/-1/0`、MEMS 为按堆 `0..4`）
3. 电能单位：④ 中部分点位未标单位（如 `meter_add_pos`），需确认是 kWh 还是 0.01kWh

## 五、结论

1. **两份高特文件互相印证成立**：主题宏体系、30 秒周期、JSON 数字索引、压缩算法、65535 无效值、正放负充、下发流程、历史补传 —— 全部对得上 ✔
2. **唯一的实质差异是"主题形状"**：手册用示例项目的简写（`arr`/`clu`、`meter` 无型号段），项目文档用本项目的全写（`array`/`cluster`、`meter/{厂层}/{用途}/…/extend/{型号}`）。
   按手册第 1 节的说明，这属正常差异，**以项目文档为准**；平台已对两种形状都做兼容。
3. **电表是本项目里最复杂的一块**：4 个维度、多个型号后缀、45+39+35+35 个遥测点 + 各 7 个遥信点。平台已按维度+型号解析，界面上分四块面板展示（储能计量 / 并网逆流 / 需量 / 计量柜）。
4. 需要厂家提供的**只剩三样**：真实 `DeviceSN`、是否支持 TLS、一条真实报文（用来确认主题形状与电表型号）。
