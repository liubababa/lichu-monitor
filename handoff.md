# 本地连接排查 · 交接说明（handoff）

> 写于 2026-09-19。目的：说明「本地项目连不上」的现状、已经实测通过的一套参数，以及按可能性排序的排查清单。
> 相关文档：`部署指南.md`（线上部署与联调）、`refs/SJ2025B3781ESCCU-MQTT.xlsx`（高特协议点表）、`refs/高特MQTT使用手册V1.2.pdf`。

---

## 一、结论先说

这套本地链路本身是通的，我已经完整跑通过一次（含反向下发）。所以「连不上」基本不是代码坏了，而是**环境/参数**问题，最可能是下面这三条：

1. **本地三个服务没起来**：broker、CCU 模拟器、静态站点，缺一个都连不上。上一轮测试结束时我把它们关掉了；本次排查我又把三个都起好了（见第二节，可直接用）。
2. **网页里填的地址不对**：网页只能用 `ws://` 的地址。现在是**前后分离**结构，推荐填网关 `ws://127.0.0.1:9002/mqtt`（`tools/gateway.js`）；填 `mqtt://…`、裸 TCP 的 1883/1884 一定连不上（原因见第五节第 2、3 条）。直连 broker 的旧地址 `ws://127.0.0.1:9001/mqtt` 仍然可用（回滚路径），但没有登录应答与只读保护。
3. **浏览器记着上次的配置**：配置存在 localStorage（键 `dianzhan.mqtt.cfg`），如果里面还留着线上的 `wss://mqtt.ykdesign.top/mqtt` 或协议选成了「晶农EMS」，那点「连接」其实是在连线上，本地自然不通。

---

## 二、当前环境状态（实测）

| 项 | 状态 |
|---|---|
| 本机 Node | v24.19.0 |
| `tools/node_modules` | 已安装，`aedes@0.51.3`、`mqtt@5.16.0`（aedes 版本必须锁 0.51.3，见第五节第 6 条） |
| 1884 / 9001 | 本次交接时**已启动**（本地 broker：TCP 1884 给设备/模拟器，WS 9001 给网页） |
| 静态站点 8080 | 本次交接时**已启动**在 `http://127.0.0.1:8080`（`python -m http.server`） |
| 1883 | **被本机其它软件占用**：`CMAgent.exe`（Cooler Master MasterControl，PID 4068）。它自带一个能正常收发 MQTT 的 broker，见第五节第 3 条 |
| 线上 | 服务器 43.129.27.206，域名 `mqtt.ykdesign.top`，设备走 1883，网页走 `wss://mqtt.ykdesign.top/mqtt`（详见 `部署指南.md`） |

---

## 三、已验证可用的一套配置（照抄即可）

| 位置 | 填什么 |
|---|---|
| 协议下拉 | 高特 CCU（本项目） |
| 网关地址 | `ws://127.0.0.1:9002/mqtt`（前后分离网关，见 `tools/gateway.js`） |
| 端口 | 留空（地址里已经带了 9002） |
| 网关账号 / 密码 | 留空 = 只读（能看不能下发）；本地默认配置的可下发账号是 `webuser` / `webpass` |
| ProductSN | `kp23bhcpmt91n2v8` |
| 设备 SN | `SN2025B3781TEST`（要和模拟器的 `--dsn` 一致） |

**实测证据（2026-09-19）**

- 独立订阅端同时挂 TCP 1884 和 WS 9001：12 秒各收到 **158 帧 / 79 个主题**，两条通道都通。
- 浏览器（headless Edge 153，CDP 9222）打开 `http://127.0.0.1:8080/index.html`，按上表填写并连接后状态变成
  `MQTT 已连接 ｜ 页面数据源：厂家 MQTT ｜ 最近上报：10:12:42`。
- 页面渲染出真实数值：实例 75 个、SOC 80%、储能功率 115.8kW「放电中」、允许充/放 250kW、电池总节数 40 节、可充 180 / 可放 150kWh、平均温度 26.4℃、最高单体 3.312V。
- **反向下发也通**：网页发 `/kp23bhcpmt91n2v8/SN2025B3781TEST/cmd/set/emu/powerCmd` → 模拟器回 `cmd/get` → 页面日志出现「下发回读 powerCmd」。
- **启动顺序无所谓**：先点连接、后起 broker 也行。页面每 5 秒自动重连，我实测在页面报「MQTT 已断开」之后再把 broker 起起来，它自己就变成「MQTT 已连接」并开始出数，不需要刷新。

**前后分离改造后的实测（2026-09-19 晚，走网关 `ws://127.0.0.1:9002/mqtt`）**

- 页面匿名连上网关：状态 `MQTT 已连接 ｜ 页面数据源：厂家 MQTT ｜ 最近上报：10:34:14`，75 个状态键、SOC 84%、储能功率 98.5kW「放电中」正常渲染。
- 匿名下发 `cmd/set/emu/powerCmd`：网关日志 `丢弃下发（只读模式）`，连接保持不断，`/healthz` 的 `deniedPublish` +1。
- 面板填 `webuser` / `webpass` 重连后同样下发：`/healthz` 的 `msgsOut` +1，模拟器回读 `10:34:00 ↓ …/cmd/get/emu/powerCmd · 下发回读 powerCmd` 出现在页面日志。
- 网关自身的端到端测试：`node tools/test-gateway.js` → 13 项断言全过（登录应答、双向桥接、只读、快照、防环、错误密码）。

---

## 四、启动步骤（四个窗口）

```bash
# 窗口 1：本地 broker（TCP 1884 + WS 9001）
cd H:\dianzhan\tools
node local-broker.js

# 窗口 2：前后分离网关（网页连这里；上游指向本地 broker）
node gateway.js --upstream mqtt://127.0.0.1:1884 --listen 9002

# 窗口 3：高特 CCU 模拟器（周期 5 秒便于观察；正常按协议是 30 秒）
node ccu-simulator.js --url mqtt://127.0.0.1:1884 --dsn SN2025B3781TEST --interval 5

# 窗口 4：静态站点（index.html 不要用 file:// 双击打开，用 http 起）
cd H:\dianzhan
python -m http.server 8080 --bind 127.0.0.1
```

然后浏览器打开 `http://127.0.0.1:8080/index.html` → 右上角数据源（显示「演示数据」的那个胶囊）→ 按第三节填 → 点「连接」。
网关的日志直接打在窗口 2（登录应答、网页接入、只读拦截都记在这里），健康检查 `curl http://127.0.0.1:9002/healthz`。

**注意**：面板是固定定位、内部可滚动的，窗口高度小的时候底部按钮（连接/断开/保存配置）会被滚出可视区域，看着像"没有连接按钮"。在面板内部往下滚一下即可。

---

## 五、排查清单（按可能性排序）

1. **三个服务是否都在**：`netstat -ano | findstr "LISTENING" | findstr ":1884 :9001 :8080"`，应该有 1884、9001、8080 三条（1884/9001 应该是同一个 PID）。
2. **地址必须是 ws://**：浏览器只能用 WebSocket（`ws://` / `wss://`），不能直连 `mqtt://`，也不能连裸 TCP 的 1883。页面里填裸主机名会自动补 `ws://`，但端口补错了一样连不上。
3. **别用 1883**：本机 1883 已经被 `CMAgent.exe`（Cooler Master 的控制中心软件）占用，而且它真的是一个能用的 MQTT broker（我实测能 CONNACK、能收发）。所以如果谁把模拟器的 `--url` 指到 `mqtt://127.0.0.1:1883`，模拟器会**显示"已连接"并正常发数据**，但数据全进了 Cooler Master 的 broker，网页（连的是 9001）永远收不到 —— 表现为"都连上了就是没数据"。判断方法：`netstat -ano | findstr ":1883"` 看 PID 是不是 4068 的 CMAgent。
4. **浏览器里有旧配置**：F12 → Console → `localStorage.removeItem('dianzhan.mqtt.cfg')` 然后刷新，或把「协议/地址/SN」按第三节重新填一遍再点「保存配置」。
5. **端口被旧进程占着**：`node tools/local-broker.js` 若报 `EADDRINUSE`，说明上一次的 broker 还活着（可能是之前的实验残留）。此时 `netstat` 仍显示端口在监听，很容易误判成"我已经起来了"。查 PID 后 `taskkill /pid <PID> /f` 再起。
6. **aedes 版本**：必须是 `0.51.3`。这台机器 Node 24 上装到 aedes 1.x 会出现"TCP/WS 能连上但始终收不到 CONNACK"的怪象。检查：`cd tools && node -e "console.log(require('aedes/package.json').version)"`。
7. **本机代理**：本机走 Clash（`127.0.0.1:7890`），一般不影响 127.0.0.1 直连；但若浏览器或命令行工具被强制走代理，localhost 也可能被代理吞掉，必要时把 127.0.0.1 加进代理白名单。

---

## 六、已知遗留问题（不影响连接，但看着不对）

1. **堆状态误报「异常」**：协议里 `arrStatus`（电池堆运行状态）的枚举是 `0 停机 / 1 充电 / 2 放电 / 3 待机 / 4 故障`（见 `refs/SJ2025B3781ESCCU-MQTT.txt` 第 149 行），但 `js/gaote-view.js:141` 只把 0 当「正常」、其余一律显示「异常」，所以充放电时会显示"堆 0 异常"。修的话建议把枚举带进点表（`tools/gen-gaote-points.py` 目前只导出 i/n/k/t/src，没导出 enum）。
2. **高特模式下残留晶农字样**：协议切到高特后，配置面板底部的厂家行和启动日志仍写着「中和汇能（山东）电气科技有限公司 · 晶农EMS」（`js/mqtt-ui.js:687` 与 `:760`）。
3. **【需补充】本次没复现"连不上"的原始现场**：以上是排查结论与可能原因清单。如果你手上有报错截图、页面当时的状态文案（顶栏胶囊显示什么）、或 F12 Console 的报错，贴进来我可以直接定位到具体哪一条。
