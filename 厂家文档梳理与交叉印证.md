# 厂家提供文件梳理与交叉印证

> 整理日期：2026-09-17　用途：确认四份文档之间的关系、哪些能互相印证、哪些互相矛盾

## 一、文件清单

| # | 文件 | 出具方 | 性质 | 核心内容 |
|---|---|---|---|---|
| ① | 《晶农EMS的MQTT通讯协议》.docx | 中和汇能（山东）电气科技 | **北向接口协议**（云平台对接用） | 7 个报文：登录 `zhhn/Post/Login/{SN}`（需平台回 `PostRsp`）、周期上报 `zhhn/Post/PeriodReport/{SN}`（**10 分钟**）、召测 `zhhn/Get/DevData/{SN}`（5s×60）、通道下发 `zhhn/Set/EmsSet/{SN}`、设备清单、站点信息 |
| ② | `mqtt_north.xlsx` | 中和汇能 | **①的点表** | 11 类设备：系统参数 / 储能表 / 关口表 / 变流器 PCS / 电芯 / 主控 BMS / 液冷机 / 干接点 / 消防 / 除湿机 / EMS，点位为英文 TAG（`PCS_P`、`RkCeVolt001`…） |
| ③ | 《高特MQTT使用手册》V1.2.pdf | 杭州高特电子设备股份有限公司 | **设备侧协议手册** | 主题宏 `${ProductSN}/${DeviceSN}/${arrNum}/${CluNum}/${devIndex}/${ManuModel}/${CompAlgo}`；周期 **30 秒**；遥测/遥信/遥调/遥控；支持 gzip/lz4 压缩；配置界面参数说明；下发前置流程 |
| ④ | `SJ2025B3781ESCCU-MQTT.xlsx` | 高特（随项目生成） | **本项目实际协议文档**（点表） | 项目：**中和汇能-山东125kW261kWh工商业液冷储能项目**；格式版本 V2；主题定义 65 条；遥测 **708 点** / 遥信 **343 点** / 遥调 **180 点** / 遥控 **1 点**；设备拓扑 57 个实例（5 堆 + 5 簇 + 5 PCS + 5 液冷 + 5 除湿 + 5 消防 + 多型号电表 + EMU/EMS/4G/IO） |
| ⑤ | 实物信息（厂家截图） | 现场 | — | 设备截图「云服务配置」界面；运维平台设备列表：`R250907J0038`（在线）、`R250907J0037`（离线），设备名"力储未来1-2 / 力储3-4" |

## 二、交叉印证结果

### ✅ 能对上的（互相印证成立）

| 印证点 | ① 晶农 docx / ② 晶农点表 | ③ 高特手册 / ④ 项目文档 | 结论 |
|---|---|---|---|
| 项目与客户 | 落款"中和汇能（山东）电气科技有限公司" | ④ 项目名"中和汇能-山东125kW261kWh工商业液冷储能项目" | **同一客户、同一项目** ✔ |
| 充放电正负 | ② 点表 `S*Power` 注释"**-充 +放**" | ③ 手册 6.8"MQTT 正负默认按 CCU 充放电正负，即**正放负充**" | **约定一致** ✔ |
| 储能量级 | — | ④ 项目 125kW/261kWh（单柜）×5 堆 | 与"力储未来1-2 / 3-4"两台 CCU 的分组吻合 ✔ |
| 设备侧配置项 | — | ③ 手册 4.1.3 参数说明（账号/密码/用户ID默认 `{deviceId}`/平台主机地址/密钥默认值/平台ID默认 0） | 与 ⑤ 设备截图上的字段**逐项对应** ✔ → 现场设备是高特 CCU |

### ❌ 对不上的（两套协议本质不同）

| 对比项 | ① 晶农北向协议 | ③④ 高特协议 | 说明 |
|---|---|---|---|
| 主题结构 | `zhhn/Post/Login/{SN}` | `/{ProductSN}/{DeviceSN}/rtg/data/{维度}/…` | 完全不同，**不能互相推算** |
| 登录握手 | 有（平台必须回 `PostRsp(result=1)`，否则不上报） | **无**（连上即 30 秒周期上报） | 机制不同 |
| 上报周期 | 10 分钟 | **30 秒** | 差 20 倍 |
| 点位定义 | 中文设备类型 + 英文 TAG（②） | 数字索引 `{"0":...,"1":...}` + 维度（④） | 完全不同的点位体系 |
| 无效值 | 字符串 `"Bad"` | 数字 `65535` | 处理方式不同 |
| 下发方式 | `Set/EmsSet` + `wayName` | `/cmd/set/emu/{功能}` + 设备回读 `/cmd/get/…` | 参数结构不同 |
| 电压单位 | ② 点表"最高单体电压"未标单位（我按 mV 显示） | ④ `maxCellVol` 单位 **V**（float32） | **单位相差 1000 倍**，映射时必须换算 |

### ⚠️ 文档自身的错误与不一致（已核实）

| # | 位置 | 问题 |
|---|---|---|
| 1 | ① docx 周期上报示例 | 全角冒号 + 尾逗号 + **`time` 写在 `data[]` 数组内** → 非法 JSON |
| 2 | ① docx 通道设置示例 | 全角逗号 + 数组尾逗号 → 非法 JSON |
| 3 | ① docx 设备信息应答 | 全角冒号 + 缺逗号 → 非法 JSON |
| 4 | ① docx 站点信息应答 | 缺逗号 + **`DeviceName` 键重复** → 非法 JSON |
| 5 | ① docx vs ② 点表 | 未使用空间拼写不一致：docx `UnuserdSpace` / 点表 `UnusedSpace` |
| 6 | ① docx | 字段说明表写 `deviceName`，实际报文用 `deviceTag` |
| 7 | ① docx | `SN` / `sn` 大小写混用；`UserInfor` 应答主题无 SN 后缀且不回 `msgId` |
| 8 | ③ 手册 vs ④ 项目文档 | 维度名大小写不一致：手册 `t4GInfo` / ④ 遥测表 `t4ginfo` |
| 9 | ③ 手册示例 | `ProductSN=kp23bhcpmt91n2v8`、`DeviceSN=S230612B0125` 均为**示例值**，非本项目实际值 |
| 10 | ③ 手册 vs ⑤ 截图的密钥 | 手册 `RFyim2I3IcfK` 与截图 `RfymI2I3IcIK` 字符不一致（以设备实际为准，不要手改） |
| 11 | ④ 项目文档 | 同一电表维度对应多个型号（acrel-adl400 / adw300 / dtsd1352 / sfere / willfar / dlt645），实际取哪个由主题里的 `${ManuModel}` 决定 |

## 三、结论

1. **四份文件不是同一套协议的两种说法，而是两套独立协议的两组文档**：
   - ①② = 中和汇能的**北向接口**约定（云平台怎么跟"晶农EMS"通信）
   - ③④ = 高特 CCU 的**设备侧协议**（设备本身怎么发数据）

2. **现场实际设备走的是高特协议**，依据有三：
   - ⑤ 设备配置界面的字段与 ③ 手册 4.1.3 完全对应
   - ④ 项目文档的项目名与 ① 的客户一致（中和汇能山东项目）
   - ⑤ 设备名"力储未来1-2 / 3-4"与该站 5 堆的物理分组吻合

3. **因此对接应按 ④ 项目协议文档 + ③ 手册执行**（平台侧已按此实现：708 遥测 / 343 遥信 / 180 遥调 / 1 遥控全部解析，16 个下发主题已建表单）。
   ①② 那套（`zhhn/` 主题）目前**用不上**，除非将来接入的是晶农自产设备。

4. **两套文档唯一需要共同遵守的约定**：功率符号 **正放负充**（两边一致，平台按此显示"充电中/放电中"）。

## 四、仍需厂家书面确认（文档无法回答的）

| # | 待确认 | 影响 |
|---|---|---|
| 1 | 两台设备的 `${DeviceSN}` 是否为 `R250907J0038` / `R250907J0037` | 主题订不起来 |
| 2 | `${ProductSN}` 是否就是默认值 `kp23bhcpmt91n2v8` | 主题前缀 |
| 3 | 设备固件是否支持 **TLS（8883）** | 我们只有加密端口，不支持则要换 broker 方案 |
| 4 | 是否启用压缩上报（主题带 `${CompAlgo}`） | 启用则平台需加解压 |
| 5 | 平台主机地址是否已改为我们的服务器 | 决定数据能否到达 |
| 6 | ①② 那套 `zhhn/` 协议是否仍会使用（本站在用哪套） | 决定是否保留晶农接入层 |
