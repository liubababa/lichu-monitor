# 晶农EMS MQTT 北向协议 · 对接问题清单

| 项目 | 内容 |
|---|---|
| 收件方 | 中和汇能（山东）电气科技有限公司 |
| 提出方 | 力储未来能源综合管理系统 |
| 依据文件 | 《晶农EMS的MQTT通讯协议》2026-03-04、`mqtt_north.xlsx` 点表 |
| 验证方式 | 在真实公网 MQTT broker 上按文档逐条收发验证；并对文档示例报文做 JSON 合法性校验 |
| 结论 | **协议框架可用**：文档定义的 14 条 Topic 链路在真实 broker 上全部可达；**但文档存在 4 处报文语法错误、多处字段不一致，以及连接参数缺失，按现状无法完成真机联调** |

---

## 一、阻塞项（不解决无法联调）

### 1. 文档中 4 处示例报文不是合法 JSON —— 照抄会直接解析失败

| 报文 | 文档位置 | 问题 | 建议修正 |
|---|---|---|---|
| 周期上报 `PeriodReport` | 协议 2. 周期上报存储数据 | ① 多个字段使用**全角冒号**（`"DiskSpace"： "232348"`、`"UnuserdSpace"：`、`"netIp"：`、`"SignalStrength"：`、`"Ccid"：`、`"version"：`）② `tags` 末尾有多余逗号（`"version"： "工控机版V1.6",`）③ **`"time"` 放在了 `data[]` 数组内部**（结构错误，应在数组外与 `data` 同级） | 改为半角冒号、删除多余逗号、把 `time` 移出 `data[]` |
| 通道设置 `EmsSet` | 协议 4. EMS通道参数设置 | ① 全角逗号（`"varValue":"25"，`）② 数组尾多余逗号（`}, ]`）③ `"time"` 前缺少逗号 | 改为半角逗号、删除尾逗号、补 `]` 与 `"time"` 之间的逗号 |
| 设备信息应答 `DeviceInfor` | 协议 6. 设备信息 | ① 全角冒号（`"DeviceManufacturer"：`、`"DeviceCode"：`）② `"time"` 前缺少逗号 | 同上 |
| 站点信息应答 `UserInfor` | 协议 7. 获取电站设备名称和SN | ① `"time"` 前缺少逗号 ② **`DeviceName` 键重复**（`"DeviceName": "电站名称"` 与 `"DeviceName": "储能电站设备1"` 同名，JSON 解析后前者被覆盖） | 拆分字段名（如 `StationName` + `DeviceName`）并补逗号 |

> 影响：以上报文若按文档原文发送，任何标准 JSON 解析器都会报错；平台侧必须做特殊容错才能收下，属于不稳定隐患。

### 2. `EmsSet` 下发的通道名（wayName）取值无对照表

- 文档示例用的是**中文通道名**：`"wayName":"下设充电/放电功率"`、`"wayName":"模块主机设置"`
- 点表 `mqtt_north.xlsx` 中定义的是**英文 TAG**：`SetPower`、`SOCmax`、`SOCmin`、`S1Power`…`S10Power` 等
- 文档未说明两者的对应关系，也未给出 `wayName` 的完整取值枚举

> 影响：**通道下发与充放电策略下发（文档第 5 节"运行策略设置"）无法实现** —— 不知道 `wayName` 该填中文名还是点表 TAG。
> 请提供：《wayName ↔ 点表 TAG 对照表》，并明确 S1~S10 分段策略的段号、时间、功率分别对应哪些 wayName，以及各单位与取值符号（功率是否仍为 -充 +放）。

### 3. 缺少 MQTT 连接参数定义

文档仅说明"EMS 配置好第三方 broker 后，会向第三方 broker 发送登录请求"，但未给出：

- broker 地址与端口（是否支持 TLS，端口号）
- 用户名/密码或其它认证方式（是否为每台 EMS 独立账号）
- QoS 等级与 retain 约定
- keepalive、clientId 命名规则（是否要求以 SN 为 clientId）
- 是否要求平台侧同时提供 TCP 与 WebSocket 接入

> 影响：没有这些参数，EMS 无法接入任何第三方平台。**请提供一份标准的接入参数说明。**

---

## 二、结构性与一致性问题（会导致解析错乱或数据错位）

| # | 问题 | 文档位置 | 影响 | 建议 |
|---|---|---|---|---|
| 4 | `"time"` 位于 `data[]` 数组内部 | 协议 2 周期上报、协议 3 召测应答 | 结构错误，解析器可能把 `time` 当成一个设备数据条目 | 移至与 `data` 同级 |
| 5 | `UnusedSpace` 与 `UnuserdSpace` 拼写不一致 | 点表为 `UnusedSpace`，协议文档为 `UnuserdSpace` | 平台按点表取不到"未使用空间"，取值为 `--` | 以点表为准统一为 `UnusedSpace` |
| 6 | `EmsSet` 字段说明表写 `deviceName`，实际 payload 用 `deviceTag` | 协议 4 扩展域表格 | 字段名不一致，容易误用 | 统一为 `deviceTag`，并给出取值枚举（EMS / PCS / BMS / 系统参数 …） |
| 7 | `SN` 与 `sn` 大小写混用 | 上报报文用 `"SN"`，召测请求用 `"sn"` | 需做大小写兼容处理 | 统一命名（建议 `SN`） |
| 8 | `UserInfor` 应答 Topic 为 `zhhn/GetRsp/UserInfor`，**无 SN 后缀**，且应答不回传请求中的 `msgId` | 协议 7 | 多站/多设备场景下无法把应答与请求配对 | 应答 Topic 补 SN 后缀，或应答中回传 `msgId` |
| 9 | 周期上报与召测应答都使用 `"identifier":"PeriodReport"` | 协议 3 召测应答示例 | 平台无法区分"周期上报"与"召测应答"（只能靠 Topic 区分） | 召测应答改用独立 identifier（如 `DevData`），或在报文中附带 `msgId` |
| 10 | `deviceType` 中文名与点表设备号的对应关系未定义完整枚举 | 协议 2 与点表 | 仅示例中出现 `系统参数`/`PCS`/`BMS`，点表中还有 `电芯`/`储能表`/`关口表`/`液冷机`/`干接点`/`消防`/`除湿机`/`EMS` 等，实际报文用哪个名字未明确 | 提供《deviceType ↔ 点表设备号》完整对照表 |

---

## 三、需确认的行为约定

| # | 待确认事项 | 说明 |
|---|---|---|
| 11 | 周期上报节拍 | 文档描述"每个设备上报周期独立，此周期在 EMS 侧可配置"与"每 10 分钟发送一次"如何并存？是否指每 10 分钟把到期设备汇聚成一帧上报？该 10 分钟能否由平台远程配置？ |
| 12 | 无效点表示方式 | "接收不到的数据标记成 Bad" —— 是字符串 `"Bad"` 还是 `null`？大小写是否固定？数值型点位无效时是否同样处理？ |
| 13 | 召测应答频率 | 文档规定"发送间隔 5s、发送次数 60 次"，应答 Topic 与周期上报不同，是否可同时进行？是否会与周期上报冲突或造成设备压力？ |
| 14 | 登录与断线重连 | EMS 未收到平台 `PostRsp(result=1)` 时是否重发 Login？重试间隔多少？网络断开后是否自动重新登录？ |
| 15 | 设备清单 `DeviceInfor` | 返回的 `DeviceTag` 是否与点表设备号一致？`DeviceCode` 是否即型号？柜内设备种类是否覆盖点表全部设备类型？ |
| 16 | 通道下发反馈 | `SetRsp` 的 `errormsg` 是否有错误码规范？多个 Tag 部分成功时如何返回？ |
| 17 | 时间与时区 | 报文中 `time` 为 Unix 秒（UTC），EMS 是否保证时钟同步？是否提供 NTP？ |
| 18 | SN 规则 | SN 长度、字符集、与点表 `SN` 字段（如 `SN21881FFF0001`）格式是否一致？Topic 中的 SN 是否带 `SN` 前缀？ |

---

## 四、我方已完成的兼容（供参考）

为确保联调不被上述文档问题阻塞，平台侧已做如下容错处理，**厂家按文档原样发送报文亦可正常接入**：

- 兼容全角冒号/逗号、数组与对象的多余尾逗号
- 兼容 `SN` / `sn` 大小写差异
- 兼容 `UserInfor` 应答 Topic 不带 SN 后缀
- 兼容点值 `"Bad"`、空值、`--` 等无效表示（页面显示 `--`）
- 兼容 `identifier` 与实际业务不一致的情况（按 Topic 兜底识别）

但**通道下发（EmsSet）依赖的 wayName 对照表缺失属于硬阻塞**，无法通过容错绕过，需厂家提供。

---

## 五、验证结论摘要（附录）

**验证环境**：真实公网 broker（`mqtt://test.mosquitto.org:1883`），使用文档原文示例报文，逐条验证发布/订阅链路。

**验证结果**：

- 文档定义的 **14 条 Topic 链路全部可达**（登录请求/应答、周期上报、召测请求/应答、通道下发/应答、设备信息请求/应答、站点信息请求/应答）
- 文档 11 段示例 payload 中，**7 段为合法 JSON，4 段非法**（即上文第 1 项）
- 复现方式：`node tools/doc-conformance.js mqtt://test.mosquitto.org:1883 <SN>`

**待厂家提供后即可封闭的问题**：

1. 《wayName ↔ 点表 TAG 对照表》（含 S1~S10 策略段）
2. MQTT 接入参数说明（地址、端口、TLS、认证、QoS、clientId、keepalive）
3. 《deviceType ↔ 点表设备号》对照表
4. 修订后的协议文档（修正 4 处 JSON 语法错误与字段不一致）

---

*本清单由力储未来平台侧整理，验证脚本与原始输出可随时提供复现。*
